package com.bit.strength.stress.network.RMI;

import com.bit.strength.stress.ViewUpdater;
import com.bit.strength.stress.network.IPArgs;
import com.bit.strength.stress.network.ViewComponents;
import com.bit.strength.stress.network.NetTask;

public class LocalMain {
	public static void main(String[] args) {
		LocalUpdater updater = LocalUpdater.getUpdater();
		/*
		 * String ip = "119.75.218.70"; int port = 80; int connection = 2; int
		 * interval = 0; int type = LocalController.TCP;
		 * 
		 * NetArgs netargs = new NetArgs(type, connection, ip, port,
		 * dataFrame.getData(), interval); NetComponents components = null;
		 * NetTask netTask = new NetTask(netargs, components, updater);
		 * 
		 * // start update thread updater.start();
		 * 
		 * // start sending thread LocalController controller = new
		 * LocalController(netTask); controller.start();
		 */
	}
}
